spring:
  datasource:
    url: jdbc:h2:mem:test
    username: ruggero
    password: 1234
    driverClassName: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: true
    hibernate:
      ddl-auto: none

  h2:
    console:
      enabled: true

  flyway:
    enabled: true

